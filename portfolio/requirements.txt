# Core data science
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# SQL / database
# sqlite3 is part of the Python standard library

# Async HTTP + scraping
httpx>=0.25.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Visualization
matplotlib>=3.7.0

# R bridge (optional — graceful fallback if R is not installed)
# rpy2>=3.5.0

# Utilities
python-dateutil>=2.8.0
tqdm>=4.65.0

# Type checking support (runtime)
typing_extensions>=4.5.0
